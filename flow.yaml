name: personalised-quiz-generator
description: A simple flow to create personalised quiz
version: 1.0

model:
  name: claude-3.5-sonnet
  temperature: 0.7
  max_tokens: 500

inputs:
  - topic: user_input
    difficulty: string
    number of input: Input text from the user

outputs:
  - questions based on input
